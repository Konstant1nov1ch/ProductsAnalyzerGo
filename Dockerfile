FROM golang:alpine

# Создать директорию приложения внутри контейнера
RUN mkdir /app

# Скопировать все файлы приложения внутрь контейнера
COPY . /app

# Перейти в директорию приложения
WORKDIR /app

# Собираем приложение
RUN go build -o main ./app/cmd/

# Устанавливаем переменную окружения для порта (если ваше приложение использует порт)
ENV PORT=8080

# Запускаем приложение при запуске контейнера c определенным аргументом

CMD ["./main", "resources/product.csv"]
